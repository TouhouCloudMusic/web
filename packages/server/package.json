{
  "name": "server",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify-whitespace --minify-identifiers --minify-syntax --target bun --outfile server ./src/index.ts",
    "start": "./server"
  },
  "dependencies": {
    "@elysiajs/opentelemetry": "^1.1.5",
    "@elysiajs/swagger": "^1.1.3",
    "@touhouclouddb/database": "workspace:*",
    "edgedb": "^1.5.12",
    "edgedb-auth-solid-start": "git+https://github.com/Huliiiiii/edgedb-auth-solid-start.git",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.6.2"
  },
  "module": "src/index.js"
}
