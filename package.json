{
  "name": "touhouclouddb",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "bunx prettier --write doc *.json *.md",
    "check": "bunx prettier -c doc *.json *.md",
    "prepare": "husky",
    "pre-push": "bun run check"
  },
  "type": "module",
  "trustedDependencies": [
    "esbuild",
    "msw",
    "protobufjs"
  ],
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3"
  }
}
