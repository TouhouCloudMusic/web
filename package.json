{
  "name": "touhouclouddb",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "commit": "bun run format && bun run lint && git cz"
  },
  "type": "module",
  "trustedDependencies": [
    "esbuild",
    "msw",
    "protobufjs"
  ]
}
