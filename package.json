{
  "name": "touhouclouddb",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write . ",
    "lint": "eslint --cache",
    "commit": "bun run format && bun run lint && git cz"
  }
}
