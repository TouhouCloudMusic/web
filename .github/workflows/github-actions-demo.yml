name: GitHub Actions
run-name: TODO 🚀
on: [push]
jobs:
  TODO_Name:
    runs-on: ubuntu-latest
    steps:
      # import
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
      # test build
      - run: bun install
      - run: bun run build
      # output info
      - run: echo "${{ job.status }}"
